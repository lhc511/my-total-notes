//abc:使用的核心模块中的功能
//.const.path .= require("path")
//.const.fs .= require("fs")
// path.resolve()
// path.extname()

//.fs.readFile()


// ·/·情况二 :· 路径
// const abc = require("./abc")
// console.log(abc.name)

// 情况三 :· X不是路径也不是核心模块
// const why = require("why")

//·情况三 :· X不是路径也不是核心模块
const axios = require("axios")//使用第三方包
axios.get()

console. log(module.paths)
console.log(module.paths)