在移动端写手指跟随移动功能时，要用transform属性 this.style.transform
例如：this.style.transform='translateX('+tranlatex+'px)'  //tranlatex是变量，存放了距离

移动端click事件会有300ms的延时,原因是移动端屏幕双击会缩放(double tap to zoom)页面。解决方案:
1.禁用缩放。浏览器禁用默认的双击缩放行为并且去掉300ms的点击延迟。
在html文件开头添加属性 content="user-scalable=no"  <meta name="viewport" content="user-scalable=no">
2.利用touch事件自己封装这个事件解决300ms延迟。
原理就是:1.当我们手指触摸屏幕,记录当前触摸时间
       2.当我们手指离开屏幕,用离开的时间减去触摸的时间
       3.如果时间小于150ms,并且没有滑动过屏幕,那么我们就定义为点击
       //封装tap,解决click 300ms 延时
function tap (obj, callback) {
    var isMove = false;
    var startTime=0;// 记录触摸时候的时间变量
    obj. addEventListener ('touchstart', function (e) {
        startTime=Date.now();//记录触摸时间
    })
    obj. addEventListener ('touchmove', function (e) {
        isMove=true; //看看是否有滑动,有滑动算拖拽,不算点击
    })
    obj. addEventListener ('touchend', function (e) {
    if (!isMove && (Date.now()- startTime)<150){//如果手指没有移动并且触摸和离开时间小于150ms算点击
        callback && callback();//执行回调函数
    })
    isMove= false; // 取反 重置
        startTime = 0;
    })
}
//调用
tap(div,function(){ //执行代码 });

第三种方法:直接引用插件(即js文件)然后引入以下代码就可以清除，之后正常写就好
if ('addEventListener' in document) {
            document.addEventListener('DOMContentLoaded', function() {
                FastClick.attach(document.body);
            }, false);
        }
swiper插件使用时在js文件中的属性参数可以在网站中查阅其功能并修改
<video src="mov.mp4" controls></video>插入一个名字是mov.mp4的视频文件。controls用来让视频能够播放

如果浏览器一直刷不出来按ctrl+f5强制刷新

change事件，当某个元素发生改变的时候触发事件，比如勾选框checked，勾中或取消