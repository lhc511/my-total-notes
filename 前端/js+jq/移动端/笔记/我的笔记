触摸事件对象重点我们看三个常见对象列表:  这三个都是触摸事件(touchstart,touchend,touchmove...)的属性，
触摸列表                    说明
touches                   正在触摸屏幕的所有手指的一个列表
targetTouches             正在触摸当前DOM元素上的手指的一个列表
changedTouches            手指状态发生了改变的列表,从无到有,从有到无变化
2. window.sessionStorage  只有关闭浏览器或者手动删掉才会删除
1、生命周期为关闭浏览器窗口
2、在同一个窗口(页面)下数据可以共享
3. 以键值对的形式存储使用
存储数据 sessionStorage.setltem(key, value)

localStorage   只有手动删除才会从数据中清除，关闭浏览器也不会删除
3. window.localStorage
1、生命周期永久生效,除非手动删除否则关闭页面也会存在
2、可以多窗口(页面)共享(同一浏览器可以共享)
3. 以键值对的形式存储使用
存储数据:localStorage.setltem(key, value)
获取数据:localStorage.getltem(key)
删除数据:localStorage.removeltem(key)
删除所有数据:localStorage.clear()

